---
title: "Report"
format: docx
---

We can use `{targets}` in Quarto by reading the R objects with `tar_read()`.

```{r}
#| echo: true
library(targets)
tar_read(summed_data)
```

And not we plot the data

```{r}
#| echo: true
tar_read(plot)
```

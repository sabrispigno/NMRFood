---
title: "Report"

format: docx
execute:
  echo: false
---

We can use '{targets}' in Quarto by just reading the R objects into de document with 'tar_read()'

```{r}
library(targets)
tar_read(data)
```

And not we plot the data

```{r}
tar_read(plot)
```
